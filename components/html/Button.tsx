import { styled } from "@stitches/stiches.config"

export const Button = styled("button", {
  boxShadow: "0 0 0 15px $black",
  backgroundColor: "$primary",
  color: "$offWhite",
  font: "$untitled",
  fontSize: "$md",
})
